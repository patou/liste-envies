package fr.desaintsteban.liste.envies.enums;

/**
 * How can we share the list, the privacy
 */
public enum SharingPrivacyType {
    PRIVATE, // Only share to specific people
    OPEN,  // Can share by link, people, can participate by automatically add it into all
    PUBLIC // public list.
}
