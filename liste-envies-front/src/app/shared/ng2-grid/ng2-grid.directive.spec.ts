import { Ng2GridDirective } from './ng2-grid.directive';

describe('Ng2GridDirective', () => {
  it('should create an instance', () => {
    const directive = new Ng2GridDirective();
    expect(directive).toBeTruthy();
  });
});
